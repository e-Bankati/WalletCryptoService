server:
  port: 8070

eureka:
  instance:
    hostname: walletcryptoservice.onrender.com
  client:
    serviceUrl:
      defaultZone:
        ${EUREKA_HOST:https://bankatii-discovery-server.onrender.com/eureka/}
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false
