server:
  port: 8070

eureka:
  instance:
    hostname: bankati-wallet-crypto.onrender.com
  client:
    serviceUrl:
      defaultZone:
        ${EUREKA_HOST:https://bankatii-discovery.onrender.com/eureka/}
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false