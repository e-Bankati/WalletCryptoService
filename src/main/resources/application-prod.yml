server:
  port: 8070

eureka:
  instance:
    hostname: bankatii-walletcrypto-service.onrender.com
  client:
    serviceUrl:
      defaultZone:
        ${EUREKA_HOST:https://bankatii-discovery-server.onrender.com/eureka/}
    register-with-eureka: true
    fetch-registry: true
  server:
    enable-self-preservation: false
